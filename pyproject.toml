[project]
name = "traderia"
version = "0.1.0"
description = "Paper trading agent with market context, sentiment, feedback, and effectiveness metrics."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]

